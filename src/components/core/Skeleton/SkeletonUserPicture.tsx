import { Skeleton } from "@/components/ui/skeleton";
import React from "react";

const SkeletonUserProfile = () => {
  return <Skeleton className="h-10 w-10 rounded-full object-cover" />;
};

export default SkeletonUserProfile;
